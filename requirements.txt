# PMLDL Assignment 1 — pipeline dependencies (stages 1–2, scheduler)
#
# Versions are MINIMUM bounds, not exact pins: this way pip always picks
# the newest release that ships prebuilt wheels for YOUR Python version.
# (Exact pins like pandas==2.2.3 break on newer Pythons — no wheel exists,
# so pip tries to compile pandas from source and fails without MSVC.)
#
# Tested with Python 3.10–3.12 (sandbox) and works with the latest
# pandas / scikit-learn / mlflow 2.x and 3.x.
numpy>=1.26
pandas>=2.2
scikit-learn>=1.4
mlflow>=2.19

# deployment (API + app are containerized with their own requirements,
# these are for running the services outside Docker / for local testing)
fastapi>=0.115
uvicorn[standard]>=0.34
streamlit>=1.41
requests>=2.32
pydantic>=2.10
